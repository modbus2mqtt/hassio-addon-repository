import { Request } from 'express';
import * as multer from 'multer';
import { SpecificationFileUsage } from '@modbus2mqtt/specification.shared';
export interface GetRequestWithUploadParameter extends Request {
    query: {
        specification?: string;
        busid?: string;
        usage?: SpecificationFileUsage;
        slaveid?: string;
        url?: string;
    };
}
export declare function getFilenameForUpload(filename: string): string;
export declare const fileStorage: multer.StorageEngine;
export declare const zipStorage: multer.StorageEngine;
