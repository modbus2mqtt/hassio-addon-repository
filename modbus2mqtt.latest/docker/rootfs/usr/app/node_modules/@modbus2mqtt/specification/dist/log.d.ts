export declare enum LogLevelEnum {
    verbose = "verbose",
    timing = "timing",
    http = "http",
    notice = "notice",
    warn = "warn",
    error = "error"
}
export declare class Logger {
    private prefix;
    static isInitialized: boolean;
    constructor(prefix: string);
    static logLevel: LogLevelEnum;
    log(level: LogLevelEnum, message: any, ...args: any[]): void;
    private static init;
    private static forwardToConsole;
    log2File(message: any, ...args: any[]): void;
}
