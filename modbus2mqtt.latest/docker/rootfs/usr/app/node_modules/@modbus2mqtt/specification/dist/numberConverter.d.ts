import { Converters, ModbusRegisterType } from '@modbus2mqtt/specification.shared';
import { Converter, ReadRegisterResult } from './converter';
import { Ispecification, Ientity } from '@modbus2mqtt/specification.shared';
export declare class NumberConverter extends Converter {
    constructor(component?: Converters);
    modbus2mqtt(spec: Ispecification, entityid: number, value: ReadRegisterResult): number | string;
    mqtt2modbus(spec: Ispecification, entityid: number, value: number | string): ReadRegisterResult;
    getParameterType(_entity: Ientity): string | undefined;
    getModbusLength(entity: Ientity): number;
    getModbusRegisterTypes(): ModbusRegisterType[];
}
