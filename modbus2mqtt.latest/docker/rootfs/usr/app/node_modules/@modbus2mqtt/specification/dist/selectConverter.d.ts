import { Converter } from './converter';
import { Ientity, Ispecification, ModbusRegisterType, Converters } from '@modbus2mqtt/specification.shared';
import { ReadRegisterResult } from './converter';
export declare class SelectConverter extends Converter {
    length: number;
    constructor(component?: Converters);
    private getOptions;
    publishModbusValues(): boolean;
    modbus2mqtt(spec: Ispecification, entityid: number, value: ReadRegisterResult): number | string;
    mqtt2modbus(spec: Ispecification, entityid: number, name: string): ReadRegisterResult;
    getParameterType(_entity: Ientity): string | undefined;
    getModbusRegisterTypes(): ModbusRegisterType[];
}
