import { Converter } from './converter';
import { Ientity, Ispecification, Converters, ModbusRegisterType } from '@modbus2mqtt/specification.shared';
import { ReadRegisterResult } from './converter';
export declare class TextConverter extends Converter {
    constructor(component?: Converters);
    private getStringlength;
    getModbusLength(entity: Ientity): number;
    modbus2mqtt(spec: Ispecification, entityid: number, value: ReadRegisterResult): number | string;
    getModbusRegisterTypes(): ModbusRegisterType[];
    mqtt2modbus(spec: Ispecification, entityid: number, _value: string): ReadRegisterResult;
    getParameterType(_entity: Ientity): string | undefined;
}
