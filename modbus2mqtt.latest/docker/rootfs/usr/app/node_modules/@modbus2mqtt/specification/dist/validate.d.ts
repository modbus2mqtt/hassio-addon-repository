declare global {
    namespace NodeJS {
        interface ProcessEnv {
            GITHUB_TOKEN: string;
            PR_NUMBER: string;
            GITHUB_OUTPUT: string;
        }
    }
}
export {};
