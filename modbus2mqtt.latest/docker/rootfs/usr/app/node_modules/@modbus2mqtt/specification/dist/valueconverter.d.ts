import { Converter, ReadRegisterResult } from './converter';
import { Converters, Ispecification, ModbusRegisterType } from '@modbus2mqtt/specification.shared';
export declare class ValueConverter extends Converter {
    mqtt2modbus(spec: Ispecification, entityid: number, _value: string | number): ReadRegisterResult;
    getModbusRegisterTypes(): ModbusRegisterType[];
    constructor(component?: Converters);
    modbus2mqtt(spec: Ispecification, entityid: number): string | number;
}
